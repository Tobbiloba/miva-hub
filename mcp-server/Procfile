# Railway Procfile - Tells Railway how to start each service
# Only ONE will be active per deployment

# For Study Buddy API deployment:
# web: python src/api/study_buddy_api.py

# For Content Processor API deployment:
# web: python src/api/enhanced_content_processor_api.py

# For MCP Server deployment:
# web: python src/mcp/server_clean.py --transport sse --host 0.0.0.0 --port $PORT

